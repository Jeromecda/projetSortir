{% extends 'layouts/base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1>Edit Participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <br><a class="btn button secondary" href="{{ path('app_check_email') }}">Réinitialiser votre mot de passe</a>
    
    <br><a class="btn button warning" href="{{ path('app_participant_index') }}">Retour</a>
    

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
